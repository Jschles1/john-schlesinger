import type { NextPage } from 'next';
import Head from 'next/head';

const Contact: NextPage = () => {
    return (
        <div>
            <Head>
                <title>John Schlesinger - Contact</title>
                <link rel="icon" href="/favicon.ico" />
            </Head>

            <div>
                <h2>Contact</h2>
            </div>
        </div>
    );
};

export default Contact;
