import type { NextPage } from 'next';
import Head from 'next/head';

const Education: NextPage = () => {
    return (
        <div>
            <Head>
                <title>John Schlesinger - Education</title>
                <link rel="icon" href="/favicon.ico" />
            </Head>

            <main>
                <h2>Education</h2>
            </main>
        </div>
    );
};

export default Education;
